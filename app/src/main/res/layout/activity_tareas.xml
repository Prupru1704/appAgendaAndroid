<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:orientation="vertical"
    tools:context=".AlumnosActivity">

    <TextView
        android:id="@+id/textView3"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/Tareas"
        android:textAlignment="center"
        android:textColor="#9C8449"
        android:textSize="34sp" />

    <ListView
        android:id="@+id/listaTareas"
        android:layout_width="match_parent"
        android:layout_height="635dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal">

        <Button
            android:id="@+id/button7"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:backgroundTint="#9C8449"
            android:onClick="btnImportarTareas"
            android:text="@string/btnImportarTxt"
            android:textColor="#FFFFFF"
            android:textSize="20sp"
            app:icon="@drawable/agregar2"
            app:iconGravity="textEnd"
            app:iconTint="#FFFFFF" />

    </LinearLayout>
</LinearLayout>